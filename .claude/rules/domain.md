---
paths:
  - "src/domain/**/*.ts"
---

# domain 層の制約

- 純ロジックのみ。three の数学クラス（Matrix4/Vector3 等）は可、DOM・React・R3F・zustand への依存は禁止
- 新規/変更ロジックには必ず vitest の単体テストを伴わせる
- 連結幾何の主張（角度・寸法・被覆率・干渉）は実測テストで裏取りする。
  回転 0 の単一連結は存在しない（最小 31°/リンク、docs/07 §1）— 格子前提の設計はしない
